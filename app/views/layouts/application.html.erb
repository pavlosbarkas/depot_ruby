<!DOCTYPE html>
<html>
  <head>
    <title>Pragprog Books Online</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="main">
      <%= image_tag 'logo.svg', alt: 'The Pragmatic Bookshelf' %>
      <h1><%= @page_title %></h1>
    </header>
    <section class="content">
      <nav class="side_nav">
        <% if @cart %>
          <%# adding an if so that if we navigate to pages where cart isn't set in their controller, %>
          <%# such as products, we won't get an error. REMEMBER cart is set only in store controller %>
          <div id="cart" class="carts">
            <%= render_if @cart && @cart.line_items.any?, @cart %> <%# helped method in the application helper %>
          </div>
        <% end %>

        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/questions">Questions</a></li>
          <li><a href="/news">News</a></li>
          <li><a href="/contact">Contact</a></li>
        </ul>
        <footer><%= Time.now %></footer>
      </nav>

      <main class='<%= controller.controller_name %>'>
        <%= yield %>
      </main>
    </section>
  </body>
</html>
